# EPIC-004

## Title

Apply the document layout gate to `say`.

## Outcome

Keep the `say` repository clean by moving project-management markdown into `tickets/` and normalizing documentation paths to `~/...`.

## Tickets

- `DD-010` Move project-management markdown into `tickets/`
- `DD-011` Normalize documentation paths to `~/...`
- `DD-012` Update docs, changelog, and commit
